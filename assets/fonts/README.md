# SF Pro Fonts

Add Apple SF Pro `.otf` files from https://developer.apple.com/fonts/

Required families registered in `pubspec.yaml`:

- **SF Pro Text** — Regular, Medium, Semibold, Bold
- **SF Pro Display** — Semibold, Bold (for large amounts)

Until files are added, HavenTypography falls back to platform system sans-serif.
